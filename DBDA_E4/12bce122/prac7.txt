Create table CLUB_MEMBERS
(
	MEMBER_ID Varchar(10),
	MEMBER_fname Varchar(30) NOT NULL,
	MEMBER_lname Varchar(30) NOT NULL,
	member_phone number(20),
	member_add_id varchar(30),
	joining_date date,
	expiry_date date,
	m_type_id varchar(20)
	Constraint CLUB_cts1 PRIMARY KEY(MEMBER_ID)
) 



SQL> set serveroutput on;
SQL> DECLARE
  2  	CURSOR c IS select member_fname from club_members;
  3  	TYPE c_list IS TABLE OF member.member_fname%TYPE INDEX BY binary_integer;
  4  	names c_list;
  5  	cnt number:=0;
  6  
  7  BEGIN
  8  	for n in c LOOP
  9  		cnt:=cnt+1;
 10  		names(cnt):=n.member_fname;
 11  		DBMS_OUTPUT.PUT_LINE('member('||cnt||'):'||names(cnt));
 12  	END LOOP;
 13  END;
 14  /
member(1):anmol                                                               
member(2):raghav                                                            
member(3):himani                                                             

PL/SQL procedure successfully completed.

SQL> spool off;