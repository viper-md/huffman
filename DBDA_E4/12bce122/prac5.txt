Create table CLUB_MEMBERS
(
	MEMBER_ID Varchar(10),
	MEMBER_fname Varchar(30) NOT NULL,
	MEMBER_lname Varchar(30) NOT NULL,
	member_phone number(20),
	member_add_id varchar(30),
	joining_date date,
	expiry_date date,
	m_type_id varchar(20)
	Constraint CLUB_cts1 PRIMARY KEY(MEMBER_ID)
)
SQL> CREATE OR REPLACE FUNCTION totalmembers
  2  RETURN number IS
  3  	total number:=0;
  4  BEGIN
  5  	select count(*) into total
  6  	from CLUB_MEMBERS;
  7  	return total;
  8  END;
  9  /

Function created.



SQL> set serveroutput on;
SQL> DECLARE
  2  	c number;
  3  BEGIN
  4  	c:=totalmembers();
  5  	DBMS_OUTPUT.PUT_LINE('total no. of CLUBMEMBERS are:'||c);
  6  END;
  7  /
total no. of CLUBMEMBERS are:5                                                     

PL/SQL procedure successfully completed.

SQL> spool off;